# ai-safety (placeholder)

Empty placeholder per `specs/plan.md`'s Project Structure. Created only when an AI moderation/translation use case is separately approved (constitution §4: Python is reserved for approved AI use cases only). No capability is implemented or assumed by the `001-stranger-meet-offers` feature.
